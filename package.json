{
  "name": "joyful-prayer",
  "private": true,
  "version": "1.2.25",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:web": "vite build",
    "build:cap": "BUILD_TARGET=capacitor vite build",
    "sync:ios": "npm run build:cap && npx cap sync ios",
    "sync:android": "npm run build:cap && npx cap sync android",
    "sync:cap": "npm run build:cap && npx cap sync",
    "release:cap": "npm run sync:cap",
    "release:all": "npm run sync:cap",
    "preview": "vite preview",
    "test": "echo \"No tests specified yet\"",
    "lint": "echo \"No linting configured yet\""
  },
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.3.1",
    "@capacitor/local-notifications": "^8.0.2",
    "@capacitor/share": "^8.0.1",
    "crypto-js": "^4.2.0",
    "firebase": "^12.12.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.3.1",
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.3.0",
    "vite": "^5.1.0"
  }
}
